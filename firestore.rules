rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This project reads and writes Firestore through secure Next.js API routes
    // using Firebase Admin SDK, so direct client access stays closed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
